# RenEvo Utility Belt (rub)

`rub` is a utility belt of command line apps that I find useful to have in one place for easier installations.

## Install

```bash
go install github.com/renevo/rub@latest
```
